# ── App ──
NODE_ENV=development
PORT=3002
# Desenvolvimento: http://localhost:5174
# Produção: https://app.seudominio.pt (ou múltiplos separados por vírgula)
CORS_ORIGIN=http://localhost:5174

# ── Docker Compose (frontend build) ──
# Desenvolvimento: http://localhost:3002
# Produção: https://api.seudominio.pt
VITE_API_URL=http://localhost:3002

# ── Database (PostgreSQL + pgvector) ──
DATABASE_URL=postgresql://crm_user:password@localhost:5432/silvia

# ── Redis ──
REDIS_URL=redis://localhost:6379

# ── JWT ──
JWT_SECRET=change-me-to-random-64-chars
JWT_REFRESH_SECRET=change-me-to-random-64-chars-refresh

# ── OpenAI (embeddings + generation + Whisper STT) ──
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# ── Resemble AI (TTS) ──
VOICE_ENABLED=true
RESEMBLE_API_KEY=
RESEMBLE_VOICE_UUID=

# ── Z-API (WhatsApp) ──
ZAPI_INSTANCE_ID=
ZAPI_TOKEN=
ZAPI_WEBHOOK_SECRET=

# ── Email ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=
IMAP_PASS=

# ── Stripe ──
STRIPE_SECRET_KEY=sk_live_...

# ── Lex Corpus (servidor externo de jurisprudência + legislação) ──
# URL base do servidor Lex Corpus (sem barra final)
LEX_CORPUS_URL=https://lexcorpus.seudominio.pt

# ── Super Admin (first user seed) ──
SUPER_ADMIN_EMAIL=admin@lexcod.pt
SUPER_ADMIN_PASSWORD=
SUPER_ADMIN_NAME=Admin
